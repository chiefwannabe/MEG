# MEMORY.md - Long-Term Memory

This is my long-term memory. I will update this file with significant events, decisions, lessons, and insights worth keeping across sessions.

## Curated Wisdom & Important Context

- **2026-07-05**: Initialized my memory. Established workspace connection in `/home/jiganesh/Downloads/IGNOU/meg`.
- **2026-07-06**: Added "Games" button to the Hero section next to "Download PYQs" and "Download Notes" in [index.html](file:///home/jiganesh/Downloads/IGNOU/meg/index.html).
- **2026-07-14**: Implemented dynamic view switching and user profile editing system in the dashboard module, synchronizing Firestore user details and Firebase Auth profile details. Built a secure, role-restricted Admin Panel at admin/ to allow resource curation, role modifications, and analytics updates. Generated production-ready firestore.rules mapping database schemas. Completed the Student Resource System including sidebar catalog browsing, advanced search/filters, dynamic dashboard listings, and details modal cards. Implemented a fully responsive mobile/tablet experience featuring a smooth hamburger drawer menu on the homepage (with role-restricted option visibility checks) and table-to-card reflows in the Admin panel. Fully integrated Bookmarks, Reading Progress, Personal Notes, Quiz Scores, Downloads, Settings, Help panels, and "Continue Learning" redirects to Firestore collections and rules whitelists. Verified, optimized, and pushed all database security rules and regenerated game assets configuration to the remote repository.
- **2026-07-15**: Rebuilt, styled, and connected the student dashboard (`dashboard/index.html`, `dashboard/index.css`, `dashboard/index.js`) to Firebase Authentication and Firestore database backend. Added support for enrolment details and student programme selection by whitelisting keys in `firestore.rules` and initializing them on user signup. Corrected missing button class styling and added smooth transition animations for theme swapping. Updated redirection and import links across homepage, admin panels, and stylesheets to correctly target the new dashboard files. Added a permanent circular "Back to Home" button to the left of the sidebar brand logo.
- **2026-07-16**: Migrated Node/Tauri configurations, applied Kenny cursor pack globally, and fully replaced the authentication system with a username-only login UI. Users register and log in with unique usernames, which are internally mapped to `username@meg.local` to leverage Firebase Auth. Cleaned and updated database security rules in `firestore.rules` and document creation in `src/firestore.js` to ensure username uniqueness is validated against Firestore prior to account creation. Cleaned all UI displays (homepage, student dashboard, and admin panel) of email address fields, replacing them with formatted custom username labels. Added a retry mechanism (3 attempts, 500ms delay) to Firestore profile creation on user signup to prevent authentication state propagation race conditions. Fixed a permissions bug by splitting users collection read rules into private `get` and public `list` with a `limit == 1` check to enable unauthenticated username uniqueness queries. Added diagnostic logs to sign-up steps. Refactored the dashboard user profile header to display only a circular avatar (removing username and chevron) and updated the dropdown to function as a synchronized alternative navigation menu (Dashboard, Bookmarks, Settings, Logout) sharing a centralized routing logic. Replaced all initials-based fallback avatars across the website with a self-contained modern SVG user silhouette data URI, and cleaned the admin panel header button to show only the circular avatar.





