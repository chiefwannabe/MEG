{
  "name": "meg-offline-launcher",
  "version": "1.0.0",
  "description": "IGNOU MEG website with offline game launcher",
  "private": true,
  "scripts": {
    "build": "bun offline/generate-games.js && bun generate-uploads.js",
    "generate": "bun offline/generate-games.js",
    "dev": "bun server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
